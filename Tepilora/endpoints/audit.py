"""
Audit API namespace.

AUTO-GENERATED by scripts/generate_sdk.py - DO NOT EDIT MANUALLY!
Regenerate with: python scripts/generate_sdk.py --category audit
"""
from __future__ import annotations

from typing import Any, Dict, Optional

from ._base import AsyncBaseAPI, BaseAPI


class AuditAPI(BaseAPI):
    """Audit namespace: history, query."""

    def history(
        self,
        *,
        entity_type: str,
        entity_id: str,
        limit: Optional[int] = 50,
        options: Optional[Dict[str, Any]] = None,
        context: Optional[Dict[str, Any]] = None,
        response_format: Optional[str] = None,
    ) -> Any:
        """Get the audit history for a specific entity (portfolio, client, query)

        Returns the chronological audit trail for a specific entity identified by its ID. Shows all operations performed on that entity: creation, modifications, deletions, access events. Each entry includes timestamp, action, user, and change details. Used by the Dashboard for entity history views (e.g. 'who modified this portfolio and when') and by compliance workflows tracking data lineage."""
        _payload: Dict[str, Any] = {}
        _payload["entity_type"] = entity_type
        _payload["entity_id"] = entity_id
        if limit is not None:
            _payload["limit"] = limit
        return self._call("audit.history", params=_payload, options=options, context=context, response_format=response_format)

    def query(
        self,
        *,
        action: Optional[str] = None,
        entity_type: Optional[str] = None,
        entity_id: Optional[str] = None,
        user_name: Optional[str] = None,
        start_date: Optional[str] = None,
        end_date: Optional[str] = None,
        limit: Optional[int] = 100,
        include_staging: Optional[bool] = True,
        options: Optional[Dict[str, Any]] = None,
        context: Optional[Dict[str, Any]] = None,
        response_format: Optional[str] = None,
    ) -> Any:
        """Query the audit trail log with filters for action, user, and date range

        Searches the audit trail (JSONL + Parquet storage) for recorded API operations. Filterable by action (e.g. 'portfolio.create'), user apikey, date range, success/failure status, and free-text search on parameters. Returns each audit entry with timestamp, action, user, parameters, result status, duration, and credits consumed. Used for compliance reporting, usage analytics, debugging failed operations, and billing reconciliation.

        Args:
        start_date: Start date for time range queries (ISO 8601 YYYY-MM-DD)
        end_date: End date for time range queries (ISO 8601 YYYY-MM-DD)"""
        _payload: Dict[str, Any] = {}
        if action is not None:
            _payload["action"] = action
        if entity_type is not None:
            _payload["entity_type"] = entity_type
        if entity_id is not None:
            _payload["entity_id"] = entity_id
        if user_name is not None:
            _payload["user_name"] = user_name
        if start_date is not None:
            _payload["start_date"] = start_date
        if end_date is not None:
            _payload["end_date"] = end_date
        if limit is not None:
            _payload["limit"] = limit
        if include_staging is not None:
            _payload["include_staging"] = include_staging
        return self._call("audit.query", params=_payload, options=options, context=context, response_format=response_format)



class AsyncAuditAPI(AsyncBaseAPI):
    """Audit namespace: history, query."""

    async def history(
        self,
        *,
        entity_type: str,
        entity_id: str,
        limit: Optional[int] = 50,
        options: Optional[Dict[str, Any]] = None,
        context: Optional[Dict[str, Any]] = None,
        response_format: Optional[str] = None,
    ) -> Any:
        """Get the audit history for a specific entity (portfolio, client, query)

        Returns the chronological audit trail for a specific entity identified by its ID. Shows all operations performed on that entity: creation, modifications, deletions, access events. Each entry includes timestamp, action, user, and change details. Used by the Dashboard for entity history views (e.g. 'who modified this portfolio and when') and by compliance workflows tracking data lineage."""
        _payload: Dict[str, Any] = {}
        _payload["entity_type"] = entity_type
        _payload["entity_id"] = entity_id
        if limit is not None:
            _payload["limit"] = limit
        return await self._call("audit.history", params=_payload, options=options, context=context, response_format=response_format)

    async def query(
        self,
        *,
        action: Optional[str] = None,
        entity_type: Optional[str] = None,
        entity_id: Optional[str] = None,
        user_name: Optional[str] = None,
        start_date: Optional[str] = None,
        end_date: Optional[str] = None,
        limit: Optional[int] = 100,
        include_staging: Optional[bool] = True,
        options: Optional[Dict[str, Any]] = None,
        context: Optional[Dict[str, Any]] = None,
        response_format: Optional[str] = None,
    ) -> Any:
        """Query the audit trail log with filters for action, user, and date range

        Searches the audit trail (JSONL + Parquet storage) for recorded API operations. Filterable by action (e.g. 'portfolio.create'), user apikey, date range, success/failure status, and free-text search on parameters. Returns each audit entry with timestamp, action, user, parameters, result status, duration, and credits consumed. Used for compliance reporting, usage analytics, debugging failed operations, and billing reconciliation.

        Args:
        start_date: Start date for time range queries (ISO 8601 YYYY-MM-DD)
        end_date: End date for time range queries (ISO 8601 YYYY-MM-DD)"""
        _payload: Dict[str, Any] = {}
        if action is not None:
            _payload["action"] = action
        if entity_type is not None:
            _payload["entity_type"] = entity_type
        if entity_id is not None:
            _payload["entity_id"] = entity_id
        if user_name is not None:
            _payload["user_name"] = user_name
        if start_date is not None:
            _payload["start_date"] = start_date
        if end_date is not None:
            _payload["end_date"] = end_date
        if limit is not None:
            _payload["limit"] = limit
        if include_staging is not None:
            _payload["include_staging"] = include_staging
        return await self._call("audit.query", params=_payload, options=options, context=context, response_format=response_format)


